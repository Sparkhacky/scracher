# ─────────────────────────────────────────────────────
#  SCRACHER v3 — Configuración (.env)
#  Copia este fichero a .env y rellena los valores
# ─────────────────────────────────────────────────────

# ── TOR PROXY ────────────────────────────────────────
TOR_SOCKS=socks5h://127.0.0.1:9050
# Si usas Tor Browser: 9150

# ── VIRUSTOTAL (gratuito: 4 req/min) ─────────────────
# Consigue tu API key en: https://www.virustotal.com/gui/sign-in
VT_API_KEY=

# ── ALERTAS: SLACK ───────────────────────────────────
# Crea un Incoming Webhook en: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=

# ── ALERTAS: EMAIL (SMTP) ────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_email@gmail.com
SMTP_PASS=tu_app_password
SMTP_FROM=scracher@localhost
SMTP_TO=analista@tu_org.com,supervisor@tu_org.com

# ── NIVEL MÍNIMO PARA ALERTAS ────────────────────────
# Opciones: critical / high / medium
ALERT_MIN_LEVEL=high

# ── MÓDULOS ──────────────────────────────────────────
ENABLE_OCR=true
ENABLE_THREAT_INTEL=true

# ── INTERVALOS RE-ESCANEO (horas) ────────────────────
RESCAN_CRITICAL_H=6
RESCAN_HIGH_H=12
RESCAN_MEDIUM_H=24
RESCAN_LOW_H=48

# ── OCR IDIOMAS (tesseract) ───────────────────────────
# Instalar: sudo apt install tesseract-ocr-<lang>
OCR_LANGS=eng+spa+rus
